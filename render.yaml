services:
  - type: web
    name: lottery-backend
    env: node
    buildCommand: |
      cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: true
    buildFilter:
      paths:
      - backend/**/*